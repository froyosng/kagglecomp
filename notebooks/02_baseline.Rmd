---
title: "02 - Baseline Models"
author: "Team"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
library(tidyverse)
library(here)
source(here("R", "log_loss.R"))
```

# Load data and validation split

```{r load}
train <- read.csv(here("data", "train.csv"))
test  <- read.csv(here("data", "test.csv"))

# Load the respondent-level split described in cleaning_log.md
# split <- readRDS(here("data_processed", "train_val_split.rds"))
```

# Benchmark: 0.25 for every alternative

This should reproduce the competition benchmark of ~1.38629 and confirms
`log_loss()` is wired up correctly.

```{r benchmark}
# n_val <- nrow(<validation truth>)
# bench_pred <- matrix(0.25, nrow = n_val, ncol = 4)
# log_loss(bench_pred, <validation truth>)   # expect ~1.386
```

# First real model

```{r first-model}
# e.g. a basic multinomial logit on attributes only
```

# Validation score

```{r score}
# log_loss(pred_val, truth_val)
```

# Generate submission

```{r submission}
# sub <- data.frame(...)  # match sample_submission.csv columns exactly
# write.csv(sub, here("submissions", "submission_002.csv"), row.names = FALSE)
# Remember to log it in submissions_log.csv
```
