---
title: "03 - Model Experiments (consolidated)"
author: "Team"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
library(tidyverse)
library(here)
source(here("R", "log_loss.R"))
```

> This notebook is the **consolidated** record of model experiments.
> Do your own exploration in `notebooks/experiments/<yourname>_*.Rmd`,
> then fold the working results into this file **one person at a time**
> (via a branch + pull request) to avoid merge conflicts.

# Summary table of models tried

| Model | Who | Val log loss | Public LB | Notes |
|---|---|---|---|---|
| 0.25 benchmark | — | 1.38629 | TBD | sanity check |
|  |  |  |  |  |

# mlogit specifications

```{r mlogit}
```

# Mixed logit

```{r mixed-logit}
```

# Tree-based (CART / random forest / boosting)

```{r trees}
```

# Feature engineering experiments

```{r features}
```
