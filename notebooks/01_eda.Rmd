---
title: "01 - Exploratory Data Analysis"
author: "Team"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
library(tidyverse)
library(here)
```

# Load data

```{r load}
train <- read.csv(here("data", "train.csv"))
test  <- read.csv(here("data", "test.csv"))

dim(train)   # expect 21565 x 113
dim(test)    # expect 4997 x ...
```

# Structure of the data

```{r structure}
# str(train)
# names(train)
```

# Choice distribution (which alternative gets picked)

```{r choice-dist}
# colMeans(train[, c("Ch1","Ch2","Ch3","Ch4")])
# Ch4 (opt-out) is chosen ~30.2% per the cleaning log
```

# Attribute exploration

```{r attributes}
# distributions of CC, GN, ..., HU and Price across alternatives
```

# Respondent covariates

```{r covariates}
# segment, miles, age, income, etc. — see cleaning_log.md for encodings
```

# Notes / findings

- Record anything worth carrying into modeling here, and mirror key
  decisions into `cleaning_log.md`.
